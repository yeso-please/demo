# 숨은여행 (Sumeun) — 제품 요구사항 정의서 · PRD v2.0

> 랭킹 없는 지도에서 낯선 지역을 발견하고,
> 빠르게 비교한 뒤 실제로 갈 수 있는 코스로 완성하는 국내 여행 서비스
>
> **기준일** 2026.08.11 · **상태** 대회 MVP 설계안
> 함께 보기: [화면 명세서](SCREENS.md) · [데이터 제약](DATA.md) · [디자인 토큰](DESIGN-TOKENS.md)

---

## 문서 요약

### 제품 한 줄 정의

> **기억에 남은 여행을 줄글로 적으면 그 글에서 취향을 읽어, 아직 안 가본 지역 중
> 그 취향에 맞는 곳에 지도의 불을 켜고, 같은 축으로 하루 동선까지 조립해 준다.
> 다녀와서 다시 적으면 그 글이 다음 추천을 바꾼다.**

**왜 '취향'이 앞에 오는가 (2026-08-29 결정)**

이전 정의는 "낯선 지역을 실패 없이 갈 수 있게 한다"에 가까웠다. 그런데
**실패 없음·정보 충분함을 앞세우면 유명 지역이 항상 이긴다** — 강릉이 남원보다 안전하고 정보도 많다.
안전을 원하는 사용자에게는 유명한 곳을 권하는 게 맞는 답이 되어버려, 낯선 지역을 추천할 이유 자체가 사라진다.

낯선 지역이 유명 지역을 이길 수 있는 축은 **"거기가 나에게 더 맞다"** 하나뿐이다. 유명세로는 얻을 수 없기 때문이다.
따라서 두 가치의 순서를 고정한다.

| | 역할 |
|---|---|
| **취향 적합** | 왜 이 지역인가 — **선택의 이유(주)** |
| **완성된 코스** | 그래서 갈 수 있다 — **실행의 조건(종)** |

코스는 위험을 없애는 장치가 아니라 **취향이 실제로 구현된 모습을 보여주는 증거**다.
"골목이 좋으셨다면, 여기 골목만 이어진 하루가 있습니다" — 코스가 곧 추천 근거의 증명이 된다.

| 항목 | 결정 |
|---|---|
| 핵심 문제 | 공공 관광데이터는 충분하지만, 검색·인기순 중심 채널 때문에 낯선 지역이 여행 후보에 오르기 어렵다. |
| 핵심 사용자 | 새로운 국내 여행지를 원하지만 직접 발굴·조사할 시간은 부족한 20~30대 1~2인 여행자. |
| 차별점 | 랭킹 없는 발견 + 근거가 보이는 지역 비교 + **추천한 축 그대로 조립되는 코스**(아래 제품 원칙 6). |
| AI 역할 | 추천 이유를 자연어로 설명하고, 검증된 장소·코스 ID를 연결한다. 사실·공간·가격은 DB와 계산 로직이 보증한다. |
| North Star | **낯선 지역 실행 전환율** — 신규 사용자가 미방문 지역을 발견하고 코스를 저장한 비율. 2026-08-30 부터 **루프 완주**(다녀와서 다시 적음)를 함께 센다(§11.1). |
| 대회 MVP | 여행 다이어리 입력, 목적지 없는 발견, 지역 비교, 공식/자동 조립 코스, 저장·[다녀왔어요], 지도 공유, 기본 추천 근거, 이벤트 로깅. |
| 비범위 | 영상 숏폼, 자체 예약, 실시간 행동 재학습, Contextual Bandit, 완전한 운영시간 검증. |

### 이번 개정의 핵심

- 과거 논의 과정(구형 챗봇 중심 설계, "파인튜닝 불필요" 문구, 공식 코스 단독안)을 본문에서 제거하고 **현재 결정만** 남겼다.
- "모든 지역이 같은 크기"를 **"모든 지역에 동일한 탐색·클릭 권한"** 으로 수정했다. 실제 폴리곤 면적은 다르다.
- 검색량 데이터가 없으므로 **"비유명/저평가"를 임의 판정하지 않는다.** MVP에서는 미방문성·콘텐츠 준비도·서비스 내 노출 균형을 사용한다.
- TourAPI 공식 코스의 실측 커버리지 부족을 반영해 **공식 코스와 자동 조립 코스를 동일 스키마**에서 다룬다.
- 여행 MBTI는 **선택형 재미 요소**로 내리고, 추천의 핵심 입력은 방문 지역·만족도·경험 태그로 교체했다.
- 제품 경험을 **발견 → 판단 → 실행**의 세 단계로 재정의했다. 데이터가 많다는 사실보다 사용자가 하나의 여행지를 선택하고 코스를 완성하는지가 우선이다.
- 긴 온보딩 대신 **빠른 조건 칩과 점진적 질문**을 기본 입력으로 둔다. 방문 이력과 Traveler DNA는 반복 사용을 위한 선택형 개인화 경로다.
- 착한가격업소는 핵심 차별점으로 주장하지 않는다. 데이터 품질과 사용자 반응이 확인된 경우에만 가격 근거·필터로 노출한다.

### 2026-08-30 개정 — 입력을 '고르기'에서 '쓰기'로

이전 개정까지의 입력은 **조건 칩 + 방문 지역 선택 + 만족도**였다. 셋 다 *고르는* 입력이라
사용자에게 남는 게 없고, 우리에게도 12개 태그의 on/off 밖에 안 남았다.

- **여행 다이어리가 유일한 입력이 됐다.** 지역을 고르고 줄글을 쓰면 그 글에서 경험을 읽고,
  **읽은 자리에 밑줄을 그어** 근거를 드러낸다. 틀린 건 눌러서 뺀다. 태그를 고르게 하면 그건 설문이다.
- **다이어리와 후기를 한 객체(`Diary`)로 합쳤다.** 둘 다 '어느 지역에 대해 쓴 글'인데 나눠 두니
  후기는 취향으로 안 읽히고 다이어리는 사진·공유가 없었다. `savedCourseId` 가 붙은 편이
  **우리 추천으로 실제 다녀와서 쓴 글**이고, 추천이 맞았는지 알 수 있는 유일한 증거다.
- **편 수를 강제하지 않는다.** 이전 판의 '3편 이상'은 근거 없는 숫자였다 —
  실측하니 3→4→5→6편에서 후보가 계속 절반씩 바뀌어 "3이면 충분"이 성립하지 않았다.
  대신 화면이 **몇 편으로 읽은 결과인지** 밝힌다.
- **비로그인도 쓴다.** 첫 가치 경험을 로그인 뒤로 미루면 그 자리에서 이탈한다.
  세션에 담고, 로그인하는 순간 사용자에게 옮긴다.
- **공유의 단위는 코스가 아니라 지도다.** `/u/{닉네임}` — 코스를 공유하면 정보고,
  지도를 공유하면 사람이다. 불의 모양만 봐도 그 사람의 여행 결이 읽힌다.
- **걷어낸 화면**: 맞춤 여행안(조건 폼→후보 3안) · 여행 DNA 결과 화면 · 오늘의 숨은 여행지 ·
  MBTI 12문항(경로에서 제외, 프로필 뱃지로만 유지). 자세한 이유는 §5.1과 부록 B.

---

## 1. 제품 배경과 문제 정의

### 1.1 문제 관찰

개발 DB 실측 결과, 250개 시군구 **모두**에 관광지가 존재한다. 관광지 6,769곳, 음식점 8,540곳,
착한가격업소 12,106곳이 이미 수집되어 있다. 따라서 핵심 문제는 "데이터 부재"보다
**"여행자가 낯선 지역을 발견하고 선택할 수 있는 소비 경험의 부재"** 에 가깝다.

기존 포털·SNS·여행 서비스는 목적지 검색과 인기·리뷰·예약 신호를 중심으로 탐색을 시작한다.
이 구조는 이미 알려진 장소를 빠르게 찾는 데는 유리하지만, 사용자가 이름조차 모르는 지역을
후보에 올리는 데는 약하다. 다만 이 인과관계와 사용자 수요는 **아직 실제 사용자 조사로 검증되지 않았으며**,
본 PRD에서는 핵심 가설로 취급한다.

### 1.2 이해관계자별 문제

| 대상 | 현재 문제 | 숨은여행이 제공할 가치 |
|---|---|---|
| 여행자 | 목적지가 정해져야 검색을 시작할 수 있고, 낯선 지역은 정보·가격·동선 불확실성 때문에 포기한다. | 목적지 없는 지도 탐색, 빠른 조건 입력, 비교 가능한 근거 카드, 코스 저장·길찾기. |
| 지역 소상공인 | 가격·메뉴를 공개해도 기존 검색 상위 노출을 얻기 어렵다. | 시군구 상세와 자동 조립 코스 안에서 착한가격업소를 사실 기반으로 노출한다. |
| 지자체·공공기관 | 개방 데이터가 실제 여행 탐색과 행동으로 이어지는 소비 지점이 부족하다. | SIG_CD 기반으로 데이터를 통합하고 지역 발견·코스 실행 이벤트로 연결한다. |

### 1.3 공통 데이터 축

관광지·음식점·착한가격업소·지역 경계·코스를 **시군구 코드(SIG_CD)** 로 통합한다.
SIG_CD는 이해관계자의 가치를 자동으로 보장하는 사업모델이 아니라,
서로 다른 데이터를 같은 지역 경험으로 조립하게 하는 **핵심 데이터 키**다.

> **핵심 가설**
> 목적지가 정해지지 않은 여행자에게 "랭킹 없는 발견 → 과거 여행 기반 개인화 → 검증 가능한 지역 정보
> → 코스 저장·길찾기"를 하나의 흐름으로 제공하면, 사용자는 기존에 방문하지 않은 지역도
> 실제 여행 후보로 채택한다.

---

## 2. 목표와 비목표

### 2.1 제품 목표

1. 목적지 없이도 지역 탐색을 시작할 수 있게 한다.
2. 빠른 조건 입력만으로 사용자가 실제로 선택할 수 있는 후보를 만든다.
3. 후보를 비교할 때 추천 이유·데이터 출처·누락 정보를 명확히 보여준다.
4. 발견한 지역을 시간·거리·장소 구성에 맞는 코스로 연결하고 저장·공유하게 한다.
5. 반복 사용자를 위해 방문 이력과 행동 기반 개인화를 점진적으로 추가한다.
6. 지역 노출과 사용자 행동을 측정해 추천 모델의 후속 학습 기반을 만든다.

### 2.2 비목표

- 모든 여행 예약·결제를 자체 구축하지 않는다.
- 인기순·평점순·숨은 점수 순위를 사용자에게 제공하지 않는다.
- LLM이 존재하지 않는 장소, 가격, 운영시간, 이동거리를 생성하게 하지 않는다.
- 대회 MVP에서 숏폼 영상 플랫폼이나 대규모 소셜 피드를 구축하지 않는다.
- 검색량·방문자 데이터가 없는 상태에서 "전국 비유명 지역"을 객관적으로 판정했다고 주장하지 않는다.

---

## 3. 핵심 사용자와 사용 맥락

### 3.1 Primary Persona

20~30대 1~2인·커플 국내 여행자. 국내 여행 경험이 누적되어 새로운 곳을 원하지만,
낯선 지역을 직접 발굴하고 관광지·음식·가격·동선을 여러 서비스에서 조사할 시간은 부족하다.
주말이나 연휴 1~3주 전, **목적지를 정하지 않은 상태**에서 탐색을 시작한다.

### 3.2 핵심 Job-to-be-Done

> **JTBD**
> "이번 여행에서 어디로 갈지 모르겠을 때, 내가 예전에 좋아했던 경험과 연결되면서도
> 처음 가보는 지역을 부담 없이 발견하고, 실제로 사용할 코스까지 빠르게 만들고 싶다."

### 3.3 핵심 사용 상황

사용자는 긴 설문을 작성하러 오지 않는다. 다음과 같이 짧은 선택만으로 탐색을 시작한다.

```text
이번 주말 어디 갈까?
  [서울 출발] [부산 출발] [현재 위치]
  [반일] [당일치기] [1박 2일]
  [조용한 곳] [바다] [산책] [맛집] [역사]
  [차 있음] [대중교통]
```

선택하지 않은 값은 합리적인 기본값으로 처리하고, 결과를 보여준 뒤 필요한 질문만 추가한다.
방문 이력 입력은 개인화 품질을 높이는 선택형 경로이며, 첫 가치 경험을 막아서는 안 된다.

### 3.4 검증이 필요한 사용자 가설

| 가설 | 위험 | 검증 |
|---|---|---|
| 반복 여행자는 새로운 국내 지역을 원한다. | 전체 수요 가설이 틀리면 제품의 필요성이 약해진다. | 15~20명 문제 인터뷰 및 최근 여행지 선택 과정 조사. |
| 방문 지역 3~7개를 입력할 의향이 있다. | 온보딩 이탈이 높으면 개인화 엔진이 시작되지 않는다. | 단계별 완료율, 3개 입력 시점 이탈률. |
| 랭킹 없는 지도가 판단 불가로 느껴지지 않는다. | 사용자가 익숙한 지역만 누를 수 있다. | 지도/리스트 프로토타입 비교, 첫 선택 시간과 신규 지역 선택률. |
| 착한가격업소가 낯선 지역 선택의 근거가 된다. | 관심이 낮으면 핵심 데이터 가치가 약하다. | 가격 항목 노출 대비 열람·코스 담기율. |
| 자동 조립 코스가 실행 의도를 만든다. | 코스 품질이 낮으면 발견이 저장으로 이어지지 않는다. | 공식/자동 조립 코스 블라인드 선호 비교. |
| 짧은 조건 입력만으로도 후보를 고를 수 있다. | 조건을 많이 물으면 검색보다 번거로워진다. | 3~5개 칩 입력 후 첫 후보 선택 시간과 포기율 측정. |
| 비교·실행 정보가 범용 AI보다 유용하다. | 단순 추천은 GPT·Claude로 대체될 수 있다. | 동일 조건을 범용 AI와 비교해 실제 선택률·사실 일치율 측정. |

---

## 4. 제품 원칙

1. **발견은 열어두고, 실행은 검증한다.** 조건 검색으로 후보를 먼저 닫지 않되, 선택 후에는 데이터 신뢰도와 코스 준비도를 보여준다.
2. **랭킹은 없애되 판단 근거는 없애지 않는다.** 지역별 경험 태그, 데이터 수, 추천 이유, 출처를 제공한다.
3. **모든 시군구에 동일한 탐색 권한을 준다.** 지도 면적은 다르지만 모든 폴리곤은 클릭 가능하고 상세 진입 경로를 갖는다.
4. **AI는 의미를 이해하고, DB는 사실을 보증한다.** 생성 문장은 검증된 지역·장소·코스 ID에만 연결한다.
5. **없는 데이터가 기본이다.** 특산물·설명·공식 코스가 없을 때 섹션이 자연스럽게 축소되거나 자동 조립 경로로 대체되어야 한다.
6. **추천 축 = 코스 축.** (2026-08-29 신설 · 이 문서에서 가장 강한 제약)
   지역을 고른 축을 **그대로** 코스 조립에 넘긴다. `골목·역사`로 추천했으면 코스도 골목·역사로 짜인다.
   축이 갈라지면 *"골목이 좋다고 해서 추천했는데 코스엔 골목이 없는"* 상태가 되고, **추천 근거가 거짓말이 된다.**
   따라나오는 세 가지를 함께 받아들인다.
   - **코스를 만들 수 없는 지역은 추천하지 않는다.** 추천만 되고 실행이 안 되는 후보를 만들지 않는다.
   - 그 결과 **후보 지역 수가 줄어든다.** 이는 원칙 3과 긴장 관계이므로 남는 지역 수를 항상 숫자로 관리하고 문서에 드러낸다 — 숨기면 '새로운 쏠림'이 된다.
   - **추천 근거는 자동 검증할 수 있다.** `골목`으로 추천했다면 코스에 골목 성격의 장소가 실제로 들어갔는지 기계적으로 확인한다.
7. **학습 자체가 아니라 개선을 증명한다.** 파인튜닝 모델은 인간 평가셋에서 베이스라인을 넘을 때만 채택한다.
8. **묻지 말고 읽는다.** (2026-08-30 개정 — 이전엔 "입력은 짧게, 정보는 점진적으로"였다)
   첫 화면에서 긴 폼·회원가입·MBTI를 요구하지 않는 것은 그대로다. 다만 **짧게 묻는 것도 답이 아니었다** —
   조건 칩은 짧지만 사용자에게 아무것도 남기지 않는다. 대신 **쓰게 하고, 쓴 글에서 읽는다.**
   쓰는 행위 자체가 사용자에게 남는 것이어야 입력을 요구할 자격이 생긴다.
9. **지어낸 값을 채우지 않는다.** (2026-08-30 신설)
   모르는 것은 비워 두고 화면이 그 줄을 통째로 뺀다. 기본값으로 채우면 사용자에게는
   *누군가 그렇게 말한 사실*로 보인다 — 동행자 방의 '서울 출발·당일·자동차'가 그랬고,
   후보에 붙던 '취향을 넓히는 하루' 딱지가 그랬다. 둘 다 아무도 말한 적 없는 값이었다.
10. **결과는 영감이 아니라 다음 행동으로 끝난다.** 후보 비교에서 코스 저장·공유·길찾기까지 한 흐름으로 연결한다.
11. **발견은 지도에서 일어난다.** 온보딩도 결과도 같은 지도 위에서 처리한다.
    화면을 넘겨 결과를 보여주면 *"내가 고른 여행이 이 지역들을 불러냈다"*는 연결이 끊긴다.
    어두운 지도에 불이 켜지는 장면이 이 서비스의 대표 경험이므로, 그 장면을 우회하는 흐름을 만들지 않는다.
12. **입력과 출력은 같은 객체다.** (2026-08-30 신설)
    온보딩에서 쓰는 '기억'과 다녀와서 쓰는 '기록'은 같은 다이어리다. 나누면 각각 반쪽이 된다.
    이 원칙이 리텐션 장치를 따로 만들 필요를 없앤다 — **쓸수록 추천이 좋아지는 것 자체가 돌아올 이유**다.

---

## 5. 핵심 사용자 경험

### 5.1 신규 사용자 흐름 — 발견에서 실행까지

**지도가 홈이고, 온보딩도 결과도 같은 지도 위에서 일어난다** (2026-08-29 결정 · 제품 원칙 10).

```
/                  로그인 · [가입 없이 여행 한 편 적어보기]
   │
   ▼
/onboarding/visits  여행 다이어리  ← 이 서비스의 유일한 입력
   │   지역 고르기(검색·지도) → 줄글로 쓰기 → 글에 밑줄로 읽힌 경험 확인 → 저장
   │   편 수 자유("+ 한 편 더"). 로그인 불필요.
   ▼
/map               전국 지도에 불이 켜진다        ← 대표 경험
   │   왼쪽: '당신의 글이 부른 곳' (사진 + 지역 + 왜 여기인지)
   │   우하단: [무작위로 발견] · [지금 나갈 만한 곳]
   ├─ 지역 클릭 → /region 패널(근거)
   │                └→ /course?auto=true  하루 코스(표지 사진 + 결 한 줄 + 시간표)
   │                        └→ 저장 → /my/courses
   └─ /nearby      근교 반나절 — '언젠가'가 아니라 '지금 나갈까'

재방문:
/my/discoveries    내가 밝힌 지도 (250곳 중 N곳) → 다시 /onboarding/visits 로
/reviews           후기 · /profile 프로필(쓴 편 수 · 켠 지역 수 · MBTI 뱃지는 선택)
```

> **걷어낸 화면** (이전 기획의 잔재 · 2026-08-30)
> - `/trip/new` · `/trip/recommendations` — '맞춤 여행안'. 출발지·기간·동행·이동수단을 폼으로 묻고
>   후보 3안을 내놓던 흐름. 다이어리가 같은 일을 대신하는데 둘을 남기면 어느 쪽이 진짜인지 알 수 없다.
> - `/onboarding/dna` — 별도 결과 화면. `/map` 리다이렉트.
> - 지도의 '오늘의 숨은 여행지' — 다른 랭킹이었다.
> - `/onboarding`(MBTI 12문항) — 경로에서 빼고 프로필의 선택 항목으로만 남겼다.
>
> **동행자 공유방은 지도에 다시 붙였다** — 지도 발견 패널의 [같이 갈 사람에게 물어보기]
> → `POST /trips/from-discovery` → `/trips/{code}`. 방이 하는 일(여러 후보를 놓고 의견 모으기)은
> 후보의 출처가 조건 폼이든 다이어리든 그대로 성립한다. 화면에 보이는 후보 순서를 그대로 넘긴다.
>
> **묻지 않은 조건은 비워 둔다** — 예전에는 출발지·기간·이동수단을 폼에서 받아 방에 적었다.
> 폼이 없어진 지금 기본값(서울 출발 · 당일 · 자동차)을 채우면 참여자에게는 *방장이 정한 조건*으로
> 보이는데, 아무도 말한 적 없는 사실이다. `TripRoomView.hasContext()` 로 그 줄을 통째로 숨긴다.

**왜 '온보딩 → 바로 코스' 로 직행하지 않는가**

| | 문제 |
|---|---|
| 온보딩 직후 코스로 직행 | **재방문 흐름이 없다.** 온보딩은 한 번뿐인데 두 번째부터 할 일이 사라진다 |
| 결과를 별도 페이지에 표시 | **불이 켜지는 장면을 우회한다.** 내가 고른 지역들이 있던 그 지도에 새 불이 켜져야 연결이 생긴다 |

따라서 결과 화면을 따로 두지 않고 **지도 왼쪽 카드 영역**에 발견 카드를 얹는다. (반영 완료)

- `/onboarding/dna` 는 `/map` 리다이렉트로 남겼다 — 카드와 지도가 곧 결과다.
- 그 자리에 있던 **'오늘의 숨은 여행지'는 걷어냈다.** 선정 기준이 '수도권 시도코드 제외 +
  관광지 10곳 이상'이라, 랭킹을 없앤 게 아니라 **다른 랭킹으로 바꾼 것**이었다(제품원칙 2와 충돌).
  온보딩 전에는 "여행을 적으러 가는 문"만 두고, 온보딩 후에는 내 발견 카드가 그 자리를 쓴다.
- **가입 직후 첫 화면도 다이어리**(`/onboarding/visits`)다. MBTI 12문항은 묻기만 하고
  아무것도 보여주지 못한 채 화면 하나를 더 써서 첫 가치까지의 거리를 늘린다 — 프로필의 선택 항목으로 내렸다.

### 5.2 후보 비교와 코스 실행

후보 카드는 순위를 매기지 않고, 사용자가 선택할 수 있도록 동일한 형식의 근거를 제공한다.

| 표시 항목 | 원칙 |
|---|---|
| 추천 이유 | 닮은 경험·새로운 경험·선택 가능한 근거를 한 문장으로 표시 |
| 거리·시간 | 출발지와 이동수단을 기준으로 계산하거나 추정임을 표시 |
| 데이터 상태 | 출처·갱신 시점·설명/좌표/운영시간 누락을 표시 |
| 코스 결과 | 반일·당일·1박 2일 중 선택하고 장소 수·이동 순서를 제시 |
| 다음 행동 | `[코스 만들기]` `[저장]` `[공유]` `[길찾기]`를 명확히 제공 |

코스가 완전한 실행을 보장하지 못하면 “갈 수 있는 코스”가 아니라 **“구성 가능한 코스”**로 표현한다.

### 5.3 재방문 흐름 — 켜진 지도가 쌓인다

**먼저 인정할 것** (2026-08-29)

국내여행은 연 몇 회다. **여행 서비스의 저빈도는 구조적 특성**이고 우리만의 문제가 아니다.
트리플·마이리얼트립도 같은 빈도지만 **예약 수수료**로 그것을 견딘다 — 한 번 쓸 때 금액이 크기 때문이다.
우리는 예약을 팔지 않기로 했으므로(§2.2 비목표) 저빈도가 더 아프다.

그리고 이 서비스의 흐름은 그대로 두면 **일회성으로 끝난다**:
`지역 고름 → 추천받음 → 다녀옴 → 다시 올 이유 없음`.
이력이 바뀌지 않으면 추천도 바뀌지 않으므로 "다시 추천받기" 버튼만으로는 풀리지 않는다.

**대응 — 세 층위. 성격이 다르므로 구분해서 다룬다.**

| | 장치 | 무엇을 바꾸나 |
|---|---|---|
| ① | **시간 축(반나절·당일)** | **빈도를 실제로 올린다.** "여행"은 연 3회지만 "이번 주말 어디 가지"는 매주다 |
| ② | 다녀온 뒤 만족도 입력 | 빈도는 그대로. **재방문했을 때의 보상**(추천이 실제로 좋아짐)이지 재방문할 이유는 아니다 |
| ③ | **내 발견 지도** | **돌아올 이유**를 만든다. 어두운 전국 지도가 내 여행으로 하나씩 켜진다 |

**③을 우선한다.** 지도와 불빛이라는 기존 자산을 그대로 쓰므로 별도 개발이 거의 없고,
같은 은유가 *'발견의 순간'*과 *'내 여행 기록'* 양쪽에 동시에 작동해 컨셉이 흔들리지 않는다.
그리고 "내 지도를 채우려고" 기록하게 되므로 **②를 자연스럽게 끌고 온다.**

```
지도 재진입
├ 내 발견 지도 — 250곳 중 내가 밝힌 곳
│   "아직 8곳이에요. 242곳이 어둡습니다."
│   → 다녀온 곳 추가 → 이력이 늘고 추천이 달라진다
└ 아직 안 켜진 후보 탐색 → 코스 → 저장 / 길찾기
```

> **시각 문법이 겹치지 않게 한다.**
> `/map`(발견)에서 켜진 불 = **추천 후보**, `/my/discoveries`(내 지도)에서 켜진 불 = **다녀온 곳**이다.
> 한 화면 안에서 두 의미를 같은 색으로 쓰지 않는다.

**하지 않을 것** — 저빈도를 없애려고 예약·교통편까지 확장하지 않는다.
§2.2 비목표와 "우리는 ②어디로 갈까 단계에 있다"는 경계가 무너지고, 예약 플랫폼과 같은 자리에서 싸우게 된다.

### 5.4 여행 후 흐름

```
저장 코스 → 방문 완료 또는 만족도 입력 → (선택) 사진·후기 작성
         → 후기 상세의 "이 지역 보기 / 이 코스 담기" → 다른 사용자의 발견 루프로 연결
```

### 5.5 예외 흐름

- **설명 미적재** — 이름·주소·이미지와 구조화 지표만 제공하고 "자세히" 요청 시 수집·로딩 상태를 표시한다.
- **공식 코스 없음** — 자동 조립 코스를 제공하고 출처를 명확히 구분한다.
- **운영시간 미보유** — "운영시간 미검증" 경고를 표시하며 실행 가능성을 단정하지 않는다.
- **위치 권한 거부** — 현재 위치 기반 거리 대신 목적지 중심 정보와 외부 길찾기 연결을 제공한다.
- **AI 실패/한도 소진** — 구조화 특징 기반 추천과 템플릿 설명으로 폴백한다.

---

## 6. 기능 요구사항과 우선순위

### 6.1 P0 — 대회 MVP

> 2026-08-30 개정. F-02(빠른 조건 입력)·F-04(방문 이력 온보딩)·F-05(여행 DNA 결과 화면)은
> **F-01 여행 다이어리 하나로 대체**됐다. 폐기 사유는 부록 B.

| ID | 기능 | 핵심 요구사항 | 수용 기준 | 상태 |
|---|---|---|---|---|
| F-01 | **여행 다이어리** | 지역 선택(검색·지도) → 줄글 → 글에서 읽은 경험을 밑줄·칩으로 제시 → 만족도. 편 수 자유, 비로그인 허용. | 1편만으로 결과가 나오고, 화면이 몇 편으로 읽었는지 밝힌다. 로그인 시 세션의 편이 사용자에게 이관된다. | 완료 |
| F-02 | 랭킹 없는 전국 지도 | 약 250개 시군구 폴리곤, 클릭·호버·선택, 순위 없음. | 모든 시군구가 선택 가능하고 SIG_CD로 상세가 열린다. **점수를 화면에 내보내지 않는다.** | 완료 |
| F-03 | 개인화 발견 카드 | 순위 대신 서로 다른 이유의 카드. 사진 + 지역명 + 왜 여기인지 + 태그. | 방문 지역 제외, 카드마다 다른 근거 문장, 사진 없는 지역은 사진 영역 자체를 뺀다. | 완료 |
| F-04 | 지역 상세 | 관광지·음식점·선택적 가격 근거·특산물·코스 통합. | 출처·갱신 상태를 표시하고 누락 섹션은 안전하게 축소한다. | 완료 |
| F-05 | Course Matcher | 공식 코스와 자동 조립 코스를 동일 스키마로 매칭. | 표지 사진 + 결 한 줄 + 시간표. 자리마다 사진·한 줄 설명·머무는 시간. | 완료 |
| F-06 | 내 코스 편집 | 후보 담기, 삭제, 순서 변경, 타임라인. | 저장 후 장소 순서·코스 출처·예상 시간이 유지된다. | 완료 |
| F-07 | **[다녀왔어요]** | 저장한 코스에서 다이어리로. 그 편에 `savedCourseId` 가 붙는다. | 코스의 지역이 이미 선택된 채로 열린다. 소유자 확인. **큰 루프가 닫히는 지점.** | 완료 |
| F-08 | 내 지도 | "250곳 중 N곳" · 켜진 지역 · 남은 어둠. | 지도에 못 붙는 지역은 세지 않아 숫자와 그림이 일치한다. | 완료 |
| F-09 | 지도 공유 | `/u/{닉네임}` — 로그인 없이 남의 켜진 지도를 본다. 결 태그 5개. | 공개한 편만 담고, 비공개 편은 **불도 켜지 않는다.** 켜진 곳을 누르면 그 글로 간다. | 완료 |
| F-10 | 다이어리 피드 | 공개된 편 최신순. 카드 제목은 코스명이 아니라 **지역**. | 코스로 다녀온 편에 `코스대로 다녀옴` 표시. 비공개 편은 작성자에게만. | 완료 |
| F-11 | 근처 | 거리로 **먼저** 자르고 그 안에서 취향으로 고른다. | 지도(취향 우선)와 반대 순서임을 코드와 문서에 명시한다. | 완료 |
| F-12 | 실행 연결 | 카카오맵 길찾기, 링크 공유, 코스 복제. | 클릭 이벤트가 사용자·지역·코스 ID와 함께 저장된다. | 완료 |
| F-13 | 함께 고르기 | 지도의 후보들을 방으로. 가입 없이 닉네임 투표. | **묻지 않은 조건은 적지 않는다**(원칙 9). 방향 딱지도 붙이지 않는다. | 완료 |
| F-14 | **추천 평가용 이벤트 로깅** | 추천 요청·모델 버전·후보 순위·점수·추천 이유와 노출·상세·저장·삭제·공유·길찾기·명시적 적합도 평가를 연결한다. | `recommendationRequestId`와 `impressionId`로 추천 결과와 후속 행동을 재현한다. 노출 없는 항목은 Negative로 처리하지 않는다. 모델 비교가 가능하도록 로그 스키마와 동의·삭제 경로를 제공한다. | **설계 완료 / 구현 필요** |

### 6.2 P1 — MVP 안정화

- **다이어리 사진 업로드** — `Diary.photoPaths` 자리는 있고 화면이 없다. 피드와 남의 지도가 글만 있어 밋밋한 원인.
- **랜덤 룰렛** — 완전 무작위가 아니라 최소 데이터 품질과 코스 생성 가능성을 통과한 지역에서 선택한다.
- **선택형 여행 MBTI** — 프로필 뱃지·설명 톤에만. **추천 경로에서는 완전히 분리한다.**
- **추천 카드 사진 규칙** — TourAPI 대표 이미지를 그대로 쓰면 실내 거실·공연 포스터가 여행 사진 자리에 온다.
- Cross-Encoder 리랭커, 행동 기반 Traveler DNA 갱신.
- 추천 평가 파이프라인 — 명시적 적합도 평가, baseline 비교, 오프라인 평가 리포트, ablation study.

### 6.3 P2 — 사용자 데이터 확보 후

- Contextual Bandit을 이용한 탐색/활용 비율 조정.
- 동행자 취향 결합, 계절·날씨 문맥, 실제 방문 기반 코스 신뢰도.
- 자동 여행 리캡.
- **한국관광 데이터랩 방문자수 연동** — `apis.data.go.kr/B551011/DataLabService/locgoRegnVisitrDDList`.
  엔드포인트 실재 확인 완료(응답코드 30 = 키 미등록, 12 = 서비스 없음). **활용신청만 하면 붙는다.**
  붙으면 "숨은"을 관광지 개수로 근사하지 않고 *외지인 방문자 수*로 정의할 수 있다.
  다만 12태그 한계(§8.4)는 이걸로 안 풀린다 — 별개 문제다.

---

## 7. 데이터 현황과 제약

| 데이터 | 실측 | 제품 영향 |
|---|---|---|
| 시군구 | 250 | 전국 지도와 SIG_CD 통합 가능. |
| 관광지 | 6,769 / 250개 지역 | 구조화 특징 산출 가능. |
| 관광지 상세 설명 | **39건(0.58%)** | 의미 임베딩·합성 질의의 **임계 경로**. 약 15일 선적재 필요. |
| 관광지 이용시간 | **36건(0.53%)** | MVP에서 완전한 실행 가능성 검증 불가. 경고 필요. |
| 음식점 | 8,540 / 250개 지역 | 지역 상세와 코스 후보에 활용. |
| 착한가격업소 | 12,106 | **선택적 가격 근거**. 핵심 차별점으로 주장하지 않으며, 좌표 0건이므로 공간 최적화에서 제외. 식당 외 업종·최신성 문제를 함께 표시한다. |
| 특산물 | 62건 / 24개 지역 | 선택형 섹션. 지역 정체성의 공통 축으로 사용 금지. |
| 공식 여행코스 | 347개 | 대부분 제목 중심. |
| 경유지 보유 공식 코스 | **27개 / 14개 지역** | 공식 코스 단독으로 전국 실행 경로를 만들 수 없음. |
| 방문자·검색량 | 미보유 | 비유명/저평가의 객관적 판정 불가. |
| 사용자 행동 | 사실상 0 | 초기에는 조건 입력·후보 비교·저장·공유를 측정하고, 방문 이력 기반 학습은 이후 단계로 둔다. |

### 7.1 데이터 품질 원칙

- 모든 레코드에 `source`, `sourceUpdatedAt`, `ingestedAt`을 저장한다.
- 설명·좌표·운영시간 누락률을 데이터 신뢰도에 반영한다.
- 착한가격업소는 "지역 물가가 싸다"가 아니라 **"지정 업소의 공개 메뉴·가격"** 으로만 표현한다. 사용자 열람·담기율이 낮으면 핵심 화면에서 제거한다.
- 설명이 없는 관광지는 이름·주소 임베딩을 사용할 수 있으나 semantic confidence를 낮춘다.
- 모델 버전과 벡터 버전을 함께 저장하고 전량 재임베딩 완료 후 **원자적으로 교체**한다.

---

## 8. AI·데이터 분석 설계

### 8.1 전체 구조

> **핵심 구조**
> 공공데이터 → Region DNA → 방문 이력 기반 Traveler DNA → 순위 없는 개인화 발견 묶음
> → 공식/자동 조립 Course Matcher → 저장·공유·길찾기 → 행동 로그 → 후속 재학습

### 8.2 역할 분리

| 구성 | 담당 역할 | 학습 여부 |
|---|---|---|
| 구조화 분석 | 콘텐츠 비율, 다양성, 공간 밀집도, 코스 준비도, 데이터 신뢰도 | 규칙·통계 계산 |
| 관광 도메인 임베딩 | 취향 표현과 지역·코스 경험의 의미적 유사도 | **사전학습 모델 파인튜닝** |
| 발견 점수 | 유사도·새로움·코스 준비도·노출 균형·불확실성 결합 | 초기 수식, 후속 학습 가능 |
| LLM | 추천 근거를 자연어로 번역 | 별도 파인튜닝 없음 |
| DB·서비스 | 지역·장소·코스 ID, 가격, 출처 검증 | 학습 없음 |
| 코스 조립 | 공식 코스 우선, 없으면 DayPlanService 자동 조립 | 규칙 기반 MVP |

### 8.3 Region DNA

Region DNA는 정답 레이블이 없는 지역 특성을 억지로 예측하는 모델이 아니다.
**구조화 특징과 의미 임베딩을 결합한 표현**이다.

```
RegionDNA(r) = [ StructuredFeatures(r) ‖ SemanticEmbedding(r) ]
```

- **구조화 특징** — 관광 콘텐츠 구성비, 다양성 엔트로피, 관광지 좌표 기반 밀집도·분산도,
  음식점·가격 데이터 존재, 공식/자동 코스 준비도, 데이터 완전성·최신성.
- **의미 특징** — 관광지 상세와 지역 내 장소 설명을 관광 도메인 임베딩 모델로 인코딩하고 신뢰도 가중 집계.
- **외부 표현** — 자연·골목·시장·역사·휴식 등 경험 태그와 짧은 설명.
  내부 원시 점수나 순위는 노출하지 않는다.

### 8.4 Traveler DNA

방문 사실을 선호로 곧바로 간주하지 않는다. **만족도와 입력 구체성에 따라 가중치와 신뢰도를 다르게** 적용한다.

| 입력 수준 | 사용 표현 | 신뢰도 |
|---|---|---|
| 지역 + 만족도 + **직접 쓴 글** | 글에서 읽은 경험 태그를 등장 횟수로 가중(최대 3회) | 높음 |
| 지역 + 만족도 + 경험 태그 | Region DNA의 해당 차원 강조 | 중간 |
| 지역 + 만족도 | Region DNA 전체를 축소 가중 | 낮음 |

> **본인 진술이 배경 신호보다 세다.** 지역이 실제로 가진 경험(Region DNA)은 배경으로 깔고,
> 사용자가 쓴 글에서 읽은 경험에 1.5배 가중을 준다. 같은 단어를 여러 번 쓰면 그만큼 더 세되
> **3회에서 자른다** — 한 단어를 반복해 쓴다고 취향이 그만큼 세지는 않다.

#### 현재 표현의 한계 (실측, 2026-08-30)

**12개 태그로 250개 지역을 구분할 수 없다.**
통영 다이어리(골목·바다·카페·시장)를 넣었더니 1순위가 **대구 서구**였다.
유명해서 올라온 게 아니다 — 태그가 `산책·골목·시장`이라 **도시라면 어디나 걸린다.**

완화 조치로 넣은 것들과 그 한계:

| 조치 | 효과 | 남은 문제 |
|---|---|---|
| IDF 가중 (`산`·`길`·`시장`은 전국에 있으므로 약화) | 후보 3곳의 근거 문장이 전부 같던 현상 해소 | 축 자체가 12개인 건 그대로 |
| 기여도 기반 `matchedTags` (DNA 상위 N 이 아니라 DNA×지역 곱) | 카드마다 다른 근거 | 문장 형식이 같아 여전히 비슷해 보임 |
| 대표 태그 중복 페널티 | 결이 겹치는 후보가 연달아 오지 않음 | — |

**교체 지점은 `TravelerProfileService.regionVector()` 하나다.**
학습 기반 표현으로 갈아끼우면 위 완화 조치는 대부분 불필요해진다.

초기 Traveler DNA는 두 표현을 혼합한다.

```
A. 방문 지역 임베딩 가중 평균
   다시 가고 싶음 +1.0 · 좋았음 +0.7 · 보통 0 · 나와 맞지 않음 −0.7
B. 구조화 입력으로 만든 자연어 취향 프로필의 임베딩

TravelerDNA = α·A + (1−α)·B      초기 α = 0.6~0.8, 사용자 검증으로 선택
```

### 8.5 발견 점수와 랭킹 없는 노출

```
DiscoveryScore(u,r) = α·TasteSimilarity + β·Serendipity + γ·CourseReadiness
                    + δ·ExposureBalance − λ·Redundancy − μ·Uncertainty
```

- **TasteSimilarity** — Traveler DNA와 Region DNA의 의미·구조 유사도.
  > **컷은 절대값이 아니라 상대값이다.** 다이어리 한 편으로 만든 DNA 는 태그 서너 개짜리
  > 성긴 벡터라, 12축이 대체로 차 있는 지역 벡터와의 코사인이 구조적으로 낮게 나온다 —
  > 실측: 한 편(골목·바다·카페·시장)에서 고정 컷 0.80 을 넘는 지역이 **250곳 중 0곳**이었다.
  > 잘 쓴 글일수록 결과가 비는 셈이다. 그래서 "얼마나 닮았나"가 아니라
  > **"이 사람 기준 가장 닮은 곳에 얼마나 근접한가"**(최고 유사도의 92% 이내)로 자른다.
  > 절대 바닥값(0.45)은 무엇에도 닮지 않은 곳을 떨어뜨리는 용도로만 남긴다.
- **Serendipity** — 기존 취향과 연결되면서 새로운 경험 차원을 제공하는 정도.
- **CourseReadiness** — 공식 또는 자동 조립 코스를 제공할 수 있는 정도.
- **ExposureBalance** — 서비스 내부 노출 횟수가 적은 후보에 기회를 부여. **검색량 기반 "저평가"와 구분한다.**
- **Redundancy** — 방문 지역·최근 추천·같은 유형의 반복 페널티.
- **Uncertainty** — 설명·좌표·코스·데이터 최신성 부족 페널티.

점수는 **후보 생성과 묶음 구성에만** 사용하고 UI에서 1위~10위로 표시하지 않는다.

> **CourseReadiness 는 가점이 아니라 통과 조건이다** (2026-08-29 · 제품 원칙 6)
> 위 식에서 `γ·CourseReadiness` 를 다른 항과 더하면, 코스를 못 만드는 지역도 취향 점수가 높으면 추천된다.
> 그러면 "추천했는데 갈 수가 없는" 후보가 생긴다.
> **코스 조립 가능 여부를 먼저 판정해 통과한 지역만 점수 계산에 넣는다.**
>
> 그리고 코스를 조립할 때는 `TasteSimilarity` 를 만든 **바로 그 축**을 넘긴다.
> 지역을 고른 이유와 코스를 짠 기준이 같아야 근거 문장이 사실이 된다 —
> 화면에 쓰는 "좋았다고 한 여행의 '골목·역사'가 여기서도 나타나요" 는 코스가 그것을 보여줄 때만 참이다.
노출 묶음은 다음 네 가지로 구성한다.

| 묶음 |
|---|
| 익숙한 결, 새로운 지역 |
| 한 걸음 넓히는 지역 |
| 아직 경험 못 한 결 |
| 데이터가 찾은 새로운 가능성 |

### 8.6 Course Matcher

공식 코스만으로는 **14개 시군구만 커버**되므로, 공식 코스와 자동 조립 코스를 같은 Course DNA 스키마로
표현한다. 공식 코스가 유효하면 우선 제시하고, 없거나 경유지가 불충분하면 DayPlanService 결과를 제공한다.

- **공식 코스** — "공식 코스" 출처 배지, 원본 코스·장소 ID 유지.
- **자동 조립** — "자동 조립" 배지, 사용 규칙과 데이터 미검증 항목 표시.
- **공통 Course DNA** — 지역, 장소 유형 구성, 의미 임베딩, 예상 일정 유형, 공간 밀집도,
  착한가격업소 포함 여부, 데이터 신뢰도.
- 운영시간 커버리지가 낮으므로 MVP에서는 "실행 가능 보장"이 아니라 **"코스 구성 가능"** 으로 표현한다.

### 8.7 추천 설명과 환각 방지

| 설명 칸 | 예시 | 검증 근거 |
|---|---|---|
| 닮은 점 | 군산에서 좋았던 골목·시장 경험과 유사합니다. | 사용자 긍정 지역, 공통 태그·임베딩 근거 |
| 새로운 점 | 기존 여행에 적었던 강변 산책이 더해집니다. | 후보 지역의 차별 태그 |
| 갈 수 있는 근거 | 자동 조립 반나절 코스로 관광지 3곳과 지정 가격 업소 1곳을 연결했습니다. | 실제 지역·장소·코스 ID |

- LLM에 전체 데이터를 넣지 않고 **추천 후보와 검증된 근거만** 주입한다.
- 응답에 등장하는 지역·장소·코스 ID를 **DB에서 재검증한 뒤** 버튼을 만든다.
- 가격·운영시간·거리·존재 여부는 **생성 모델 출력값을 사실로 사용하지 않는다.**
- LLM 실패 시 템플릿 기반 3단 설명으로 폴백한다.

---

## 9. 모델 학습 계획

### 9.1 학습 목표

> **단일 학습 목표**
> 여행 취향 표현과 지역·관광지·코스 설명을 하나의 관광 경험 임베딩 공간에 배치한다.
> 범용 모델이 표면 단어는 비슷하지만 실제 경험이 다른 여행을 구분하도록 만든다.

모델은 **Bi-Encoder / Sentence-Transformer 계열, 공유 인코더 Siamese 구조**로 시작한다.
100M~500M 규모의 한국어 또는 다국어 사전학습 임베딩 모델을 후보로 비교한다.
손실함수는 **InfoNCE 또는 Multiple Negatives Ranking Loss**를 사용하고,
In-batch Negative와 Hard Negative를 함께 사용한다.

### 9.2 학습 데이터

| 단계 | 데이터 | 용도 |
|---|---|---|
| 자기지도 | 관광지 제목↔상세, 코스 설명↔하위 장소, 같은 코스의 장소 | 기본 관광 관계 학습. 공식 코스 경유지 데이터가 적어 보조적. |
| 합성 데이터 | 검증된 설명을 바탕으로 생성한 취향 질의, 부분 일치, Hard Negative | 약 250개 지역·6,769개 관광지에서 Cold Start 학습쌍 생성. |
| 인간 평가셋 | 실제 사람이 쓴 취향 문장 200~500개, 후보 적합도 0~3점 | **학습에 사용하지 않는 최종 평가 전용.** 초기 파일럿은 20~40명 paired 평가로 진행하고, 가능하면 평가자 간 일치도도 보고한다. |
| 행동 데이터 | 노출 후 저장·공유·길찾기·방문·거절 | 서비스 공개 후 Phase 2 재학습. |

### 9.3 합성 데이터 품질 규칙

- 질의에 **지역명이나 정답 장소명이 직접 포함된 샘플 제거.**
- 원문 복사, 중복, 지나치게 일반적인 문장, 원천 데이터에 없는 경험 추가 제거.
- "바다"처럼 표면 단어는 겹치지만 드라이브·어촌 휴식·해양 레저처럼 경험이 다른 **Hard Negative**를 포함한다.
- 합성 문장 생성 모델의 편향을 별도로 기록하고 Human Test와 성능 격차를 보고한다.

### 9.4 데이터 분할과 평가

문장 단위 무작위 분할은 같은 지역에서 생성한 유사 문장이 학습·평가에 동시에 들어가
성능을 부풀리므로 **금지한다.**

- **Train** — 특정 지역·관광지·코스 집합.
- **Validation** — 학습과 겹치지 않는 후보 집합.
- **Region Holdout Test** — 지역 전체를 학습에서 제외해 미지 지역 일반화 평가.
- **Human Test** — 사람의 실제 취향 문장과 0~3 관련성 레이블.
- **Temporal/User Group Split** — 같은 사용자의 미래 기록이 학습에 섞이지 않도록 시간 또는 사용자 단위로 분리한다.
- **동일 질의 비교** — 모든 모델을 같은 사용자 입력·같은 후보 풀에서 평가한다.

### 9.5 비교군과 교체 조건

| 모델 | 설명 |
|---|---|
| A. Popularity/Random | 인기순 또는 무작위 추천. 시스템 개선의 최소 기준 |
| B. BM25 | 키워드 검색 베이스라인 |
| C. 사전학습 임베딩 | 파인튜닝하지 않은 한국어/다국어 모델 |
| D. 구조화 특징 | Region DNA 구조화 차원의 유사도 |
| E. 관광 도메인 파인튜닝 | 합성·자기지도 데이터로 학습한 Bi-Encoder |
| F. 결합 모델 | E의 의미 유사도 + D의 구조화 특징 및 신뢰도 |

평가 지표: Recall@5/10, MRR, nDCG@10, Region Holdout, Human Relevance 평균과 95% 신뢰구간,
추천 다양성, 신규성, 카탈로그·지역 커버리지, 설명 납득도, 제약조건 만족률.

> **교체 조건** — D 또는 E가 **Human Test의 nDCG@10과 평균 적합도 모두에서 B를 넘을 때만**
> 서비스 모델로 채택한다. 개선이 없으면 **B+C를 유지한다.**

### 9.6 Phase 2 행동 학습

| 신호 | 예시 |
|---|---|
| Positive | 지역 저장, 코스 저장, 공유, 길찾기 실행, 방문 완료, 긍정 후기 |
| Hard Negative | 노출 후 반복 넘김, 코스 열람 직후 이탈, 저장 취소, 방문 후 부정 평가, 코스에서 장소 삭제 |
| **사용 금지 Negative** | **사용자에게 노출되지 않은 지역·코스** |

명시적 평가는 행동 로그와 분리된 정답 신호로 취급한다. 클릭·저장만으로 만족을 단정하지 않으며,
추천 후 `적합도(1~5)`, `가고 싶은 정도`, `추천 이유 납득도`를 수집한다. 추천 노출 로그에는
`recommendationRequestId`, `impressionId`, `modelVersion`, `rank`, `score`, `reasonCode`를 포함한다.

### 9.7 연구 가설·ablation·주장 범위

**연구 가설**

> 다이어리 텍스트와 관광 메타데이터를 결합한 하이브리드 모델은 인기순·키워드·단순 임베딩
> baseline보다 취향 적합도를 높이면서, 신규성과 지역 커버리지를 유지하거나 개선한다.

다음 ablation을 동일한 테스트셋에서 수행한다.

| 실험 | 제거 요소 | 확인할 기여 |
|---|---|---|
| Full | 없음 | 최종 제안 모델 |
| - Diary | 다이어리 텍스트 | 비정형 여행 기록의 개인화 기여 |
| - Location | 지역·거리 조건 | 공간 문맥의 기여 |
| - Novelty | 신규성 보정 | 숨은 지역 탐색 효과 |
| - Explanation | 추천 이유 기반 신호 | 설명 가능성·납득도 기여 |

실서비스 장기 로그가 없는 동안에는 “사용자 행동이 개선되었다”고 주장하지 않는다.
대신 고정된 Human Test와 소규모 paired 사용자 평가에서 baseline 대비 오프라인 성능과
주관적 적합도 차이를 보고한다. 인과 효과나 장기 재방문 효과는 무작위 노출 또는 충분한
종단 로그가 확보된 이후의 후속 연구로 구분한다.

행동 기반 학습은 **impression 로그를 전제**로 한다.
실제 행동 데이터가 충분히 쌓이기 전에는 학습 효과를 주장하지 않는다.

#### impression 정의 — "화면에 그려짐"이 아니라 "사용자가 봄"

Negative 는 *"보여줬는데 안 골랐다"* 여야 한다. *"아예 못 봤다"* 를 Negative 로 쓰면
모델은 사용자가 존재조차 모르는 지역을 "싫어하는 지역"으로 학습한다.

지도는 250개 폴리곤을 한 번에 렌더하므로 **"렌더됨 = 노출됨"이 성립하지 않는다.**
세션마다 250건을 기록하면 그중 대부분이 가짜 Negative 가 된다.

| 등급 | 판정 기준 | weight | 학습 사용 |
|---|---|---:|---|
| **강한 노출** | 후보 카드가 뷰포트에 **50% 이상 · 1초 이상** 보임 | 1.0 | Hard Negative 로 사용 |
| **약한 노출** | 발견 묶음에 선택되어 지도 폴리곤이 강조됨 | 0.3 | 보조 신호. Hard Negative 로 쓰지 않음 |
| **기록 안 함** | 폴리곤이 그려지기만 함 · 스크롤로 스쳐 지나감 | — | — |

- 판정은 `IntersectionObserver { threshold: 0.5 }` + 1초 체류 타이머로 한다.
  `map.js` 의 '오늘의 숨은 여행지' 무한 스크롤이 이미 같은 패턴을 쓴다.
- 한 세션에서 같은 지역이 반복 노출되면 **1회로 합치고 노출 횟수만 센다**(반복 넘김 신호로 별도 활용).
- `discovery_event.impressionId` 로 노출과 이후 행동(상세 진입·저장·길찾기)을 연결한다.
  같은 impression 에 후속 행동이 없으면 그것이 Hard Negative 후보다.

> ⚠ **impression 은 소급 복구가 불가능하다.** 저장·클릭은 DB 에 남지만
> "보여줬는데 안 고른 것"은 그 순간 기록하지 않으면 영원히 사라진다.
> Phase 2 는 나중이지만 **로깅은 서비스 오픈 전에 심어야 한다** — F-09 가 P0 인 이유다.

---

## 10. 데이터 파이프라인과 시스템 구조

### 10.1 오프라인 배치

1. TourAPI·착한가격업소·지역 경계 데이터를 SIG_CD 기준으로 정규화한다.
2. 관광지 상세 설명을 **일 950회 가드**로 선적재하고 수집 상태를 기록한다.
3. 구조화 Region DNA와 Course DNA를 계산한다.
4. 사전학습 모델로 지역·관광지·코스를 임베딩해 베이스라인 벡터를 저장한다.
5. 합성 질의와 Hard Negative를 생성·필터링한다.
6. 세라프 GPU 서버에서 임베딩 모델을 파인튜닝한다.
7. 새 모델로 전체 벡터를 재계산하고 **모델 버전 단위로 원자적 교체**한다.

### 10.2 온라인 요청

```
방문 이력 입력 → Traveler DNA 계산 → 미방문 지역 필터
             → 전체 250개 지역에 대해 발견 점수 계산 → 묶음별 후보 구성
             → 선택 지역의 공식/자동 조립 코스 매칭
             → 검증된 근거를 LLM 또는 템플릿으로 설명 → DB ID 재검증 → 화면 표시
```

### 10.3 검색·저장 방식

지역 250개, 관광지 6,769개 규모에서는 **벡터 DB가 필수적이지 않다.**
1,024차원 float32 관광지 벡터는 약 26MB이며 메모리 전수 비교가 가능하다.
초기에는 애플리케이션 메모리 또는 관계형 DB에 저장하고,
규모·지연이 실제 병목으로 확인되면 pgvector/FAISS를 검토한다.

### 10.4 주요 스키마

| 테이블 | 핵심 필드 |
|---|---|
| `visited_region` | userId, sigCd, sentiment, tags[], placeIds[], confidence, createdAt |
| `traveler_dna` | userId, vector, alpha, source, modelVersion, updatedAt |
| `region_dna` | sigCd, structuredFeatures, embedding, modelVersion, exposureBalance, dataConfidence |
| `course_dna` | courseId, origin(official\|assembled), vector, durationClass, placeIds[], dataConfidence |
| `discovery_event` | userId, sigCd, courseId, action, impressionId, weight, createdAt<br/>action: `impression_strong` \| `impression_weak` \| `detail` \| `save` \| `remove` \| `share` \| `route` \| `skip`<br/>impressionId 로 노출과 후속 행동을 연결한다 (§9.6) |
| `embedding_job` | entityType, entityId, modelVersion, sourceHash, status, updatedAt |

---

## 11. 성공 지표와 분석 설계

### 11.1 North Star

> **낯선 지역 실행 전환율**
> 신규 사용자 중 7일 안에 목적지 없이 탐색을 시작해 미방문 지역을 확인하고,
> 해당 지역의 코스를 저장하거나 공유한 사용자 비율.
>
> **분모** 빠른 조건 입력을 완료하거나 지도에서 후보 카드를 1회 이상 본 신규 사용자.
> **분자** 미방문 지역 상세 진입 후 공식 또는 자동 조립 코스를 저장·공유한 사용자.

초기 목표 20%는 **가설값**이며 파일럿의 실제 분포로 재보정한다. 개인화 온보딩 완료를 North Star의 전제조건으로 두지 않는다.
검색량 데이터가 없는 동안 "비유명 상위 20개 제외" 같은 임의 정의는 North Star에서 사용하지 않는다.

**보조 지표 — 밝힌 지역 수** (2026-08-29 · §5.3 ③)

> 사용자당 **'내 발견 지도'에서 켜진 시군구 수**와 그 증가율.

North Star 가 *한 번의 전환*을 보는 반면 이 지표는 **돌아오는지**를 본다.
1에서 2로 늘어난 사용자 비율이 재방문의 대리 지표가 된다.
다만 실사용 로그가 없는 동안에는 목표치를 세우지 않고 **설계가 존재한다는 것까지만** 주장한다.

### 11.2 퍼널 지표

| 단계 | 지표 |
|---|---|
| 진입 | 빠른 조건 입력 시작률, 3~5개 칩 완료율, 첫 후보 선택까지 걸린 시간 |
| 온보딩 | 방문 지역 3개 도달률, 완료율, 태그·장소 선택률, 여행 DNA 동의율 |
| 발견 | 묶음 노출→지역 상세 진입률, 세션당 고유 시군구 수, 미방문 지역 선택률 |
| 판단 | 후보 비교 완료율, 추천 근거 열람률, 데이터 경고 이탈률, 조건 변경률 |
| 계획 | 지역 상세→코스 확인률, 코스 저장률, 코스 수정률, 자동/공식 코스 선호 |
| 실행 | 저장→공유율, 저장→길찾기 클릭률, 코스 복제·동행자 투표율, 여행 당일 재열람률 |
| 학습 | impression 대비 positive/negative 신호율, 모델 버전별 전환 비교 |

### 11.3 대회용 실험

- **추천 실험** — 인기/무작위가 아닌 베이스라인 B와 개인화 E를 비교해 적합성·새로움·저장 의도를 측정한다.
- **코스 실험** — 공식 코스와 자동 조립 코스를 출처를 가린 상태에서 비교해 이해도·매력도·수정 필요성을 측정한다.
- **지도 실험** — 랭킹 없는 지도에서 첫 선택 시간, 탐색 지역 수, 선택 확신을 측정한다.
- **모델 실험** — A~E의 오프라인 성능과 정성 사례를 함께 제시한다. **실측하지 않은 수치는 발표에 사용하지 않는다.**

### 11.4 추천시스템 학술 평가 패키지

실서비스 운영 여부와 무관하게 추천 기술의 개선을 검증할 수 있도록 다음 평가 패키지를 P1 산출물로 고정한다.

| 구성 | 요구사항 | 산출물 |
|---|---|---|
| 가설 | 다이어리 텍스트 + 관광 메타데이터가 개인화·신규성에 기여한다는 사전 가설 | 가설과 성공/실패 판정 기준 |
| 데이터셋 | 실제 또는 평가자가 작성한 다이어리-코스 후보와 1~5 적합도 레이블 | 버전 고정된 train/validation/test 및 데이터 카드 |
| 비교군 | Popularity/Random, BM25, 사전학습 임베딩, 구조화 특징, 결합 모델 | 동일 질의·동일 후보 풀 비교표 |
| 지표 | nDCG/Recall/MRR + 다양성·신규성·커버리지·설명 납득도 | 모델별 평균, 95% 신뢰구간, 효과크기 |
| 원인 분석 | Diary·Location·Novelty·Explanation ablation | 구성 요소별 기여도 표 |
| 사용자 검증 | 같은 사용자가 모델을 모두 평가하는 paired 방식, 평가 순서 무작위화 | 적합도·가고 싶은 정도·이유 납득도 결과 |
| 재현성 | 모델 버전, 데이터 버전, random seed, 하이퍼파라미터 기록 | 실행 스크립트와 평가 리포트 |

이 패키지의 목적은 클릭률을 임의로 성과로 해석하는 것이 아니라, 동일한 조건에서
추천 알고리즘 간 차이를 검증하는 것이다. 오프라인 평가만으로 실제 장기 만족·재방문을
확정하지 않으며, 해당 주장은 실서비스 또는 종단 사용자 연구가 추가된 경우에만 사용한다.

---

## 12. 비기능 요구사항

| 영역 | 요구사항 |
|---|---|
| 성능 | 지도 초기 렌더와 지역 상세 핵심 정보는 체감 지연을 최소화한다. AI 설명은 비동기 로딩하고 구조화 정보는 먼저 표시한다. |
| 가용성 | LLM·외부 API 실패 시 템플릿 설명과 저장 데이터로 폴백한다. |
| 보안 | 운영 환경에서 H2 Console·관리자 경로 차단, 비밀번호 안전 저장, Presigned URL 사용 시 만료·권한 제한. |
| 개인정보 | 방문 이력과 행동 로그의 수집 목적을 고지하고 삭제·동의 철회 경로를 제공한다. |
| 접근성 | 색만으로 테마를 구분하지 않고 텍스트·아이콘을 병행한다. 키보드 포커스와 충분한 대비를 제공한다. |
| 모바일 | 전체 화면 지도와 바텀시트 기반 상세·필터·코스 편집 흐름을 우선 설계한다. |
| 관측성 | 모델 버전, 추천 후보, 점수 구성, LLM 오류, API 한도, 배치 성공률을 추적한다. |

---

## 13. 리스크와 대응

| 리스크 | 영향 | 대응 |
|---|---|---|
| 핵심 수요 미검증 | 제품 필요성 자체가 약해질 수 있음 | 개발과 병행해 15~20명 문제 인터뷰 및 프로토타입 테스트. |
| 온보딩 마찰 | Traveler DNA 생성 전 이탈 | **한 편만 써도 결과를 준다.** '3편' 은 근거 없는 숫자였고(3→6편에서 후보가 계속 절반씩 바뀌어 '3이면 충분'이 성립하지 않았다), 편 수를 요구하는 대신 **몇 편으로 읽은 결과인지 밝힌다.** |
| 방문=선호 왜곡 | 잘못된 취향 프로필 | 만족도·장소·태그와 confidence 사용, 결과 수정 허용. |
| 상세 설명 수집 지연 | 학습 일정 전체 지연 | P0 배치 즉시 시작, 구조화 특징만으로 W3 폴백. |
| 합성 데이터 편향 | 오프라인 성능 과대평가 | Region Holdout, Human Test, 지역명 누설 제거. |
| 공식 코스 부족 | 전국 지도와 실행 흐름 단절 | 공식 우선+자동 조립, 출처 배지, 블라인드 품질 비교. |
| 운영시간 부족 | 실행 가능성을 과장 | 미검증 경고, 외부 길찾기 연결, 표현을 "구성 가능"으로 제한. |
| 가격 데이터 오해 | 지역 전체가 저렴하다는 오인 | 지정 업소의 공개 메뉴·가격임을 명시. |
| 랭킹 없는 지도 피로 | 판단 불가·익숙한 지역 편향 | 경험 태그·발견 묶음·근거 카드로 방향 제공. |
| 후기 콜드스타트 | 피드 빈 상태 | MVP 핵심 경로에서 분리, 팀 시드·자동 리캡은 P2. |
| 범용 AI와 차별화 부족 | 사용자가 GPT·Claude를 더 편하게 느낄 수 있음 | 실제 장소 ID·출처·데이터 상태·시간/거리·코스 실행까지 연결하고 동일 조건 비교 테스트를 통과해야 한다. |
| 조건 입력 피로 | 필터가 검색보다 번거로워질 수 있음 | 칩 3~5개, 기본값, 한 질문씩 점진적 입력. 첫 결과를 막지 않는다. |
| 데이터는 많지만 결과가 약함 | 6,000개 이상의 장소가 선택 피로를 만들 수 있음 | 후보를 3개 내외로 압축하고 비교·코스·다음 행동을 한 화면에서 제공한다. |
| 착한가격업소 효용 불명확 | 관리 비용 대비 사용 가치가 없을 수 있음 | 핵심 차별점으로 주장하지 않고 열람·담기율을 기준으로 유지·격하·제거한다. |

---

## 14. 8주 실행 계획

| 주차 | 핵심 작업 | 산출물/게이트 |
|---|---|---|
| W1 | 데이터 정규화, 빠른 조건 칩, 후보 카드, 이벤트 설계 | 발견→판단 퍼널 동작, 스키마 확정 |
| W2 | 지역 비교, 반일/당일/1박 2일 자동 조립, 저장·공유·길찾기 연결 | **실행 가능한 코스 데모** |
| W3 | 공식/자동 코스 품질 점검, 데이터 상태·출처 표시, 기본 추천 baseline | 250개 지역 후보 품질 점검 |
| W4 | 선택형 방문 이력 온보딩, Traveler DNA v0, 발견 묶음 | 구조화 기반 개인화 데모 동작 |
| W5 | 실제 사용자 20~40명 paired 프로토타입 테스트, 범용 AI 비교 | 적합도·추천 이유 납득도·첫 후보 선택 기준 고정 |
| W4 | 도착 설명으로 베이스라인 임베딩, 합성 질의 생성 | A~C 베이스라인, 학습 데이터 초안 |
| W5 | Human Test 200~500건, 사용자 프로토타입 테스트 | 평가셋 고정, UX 문제 목록 |
| W6 | 세라프에서 T1 대조학습 | 모델·학습 로그·재현 설정 |
| W7 | 전량 재임베딩, Region Holdout/Human Test, baseline·ablation·신뢰구간 산출 | D~F 비교 결과, 구성 요소 기여도, 서비스 모델 결정 |
| W8 | 통합 안정화, 대회 데모·발표 자료 | 학습→분석→발견→코스→행동 데모 |

### 14.1 Go/No-Go 게이트

- **G1 데이터** — 상세 설명 수집률과 품질이 학습 문서 생성 기준을 충족하는가?
- **G2 UX** — 30초 내 조건 입력 완료율, 첫 후보 선택률, 코스 저장·공유율이 수용 가능한가?
- **G3 모델** — D/E가 Human Test에서 B를 모두 상회하는가? 아니면 B+C로 폴백한다.
- **G4 코스** — 자동 조립 코스가 블라인드 평가에서 최소 품질 기준을 통과하는가?
- **G5 데모** — 모든 추천 문장이 실제 지역·장소·코스 ID로 재검증되는가?

---

## 15. 대회 심사 기준 대응

| 심사 기준 | 숨은여행의 주장 | 증거 |
|---|---|---|
| 창의성·차별성 | 인기순을 제거한 지도 위에서 "과거 여행과 닮았지만 미방문인 지역"을 순위가 아닌 발견 묶음으로 제시. | 지도 UX, Traveler DNA, 발견 묶음 비교. |
| 대중성·실용화 | 목적지 미정 탐색부터 조건 입력·지역 비교·코스 저장·길찾기까지 한 흐름. | 30초 입력 완료율, 낯선 지역 실행 전환율, 사용자 선택 테스트. |
| 기술적 난이도 | SIG_CD 다중 데이터 통합, Region/Traveler/Course DNA, 관광 도메인 대조학습, Region Holdout, DB 검증, 오프라인·사용자 평가 파이프라인. | A~F baseline 비교, ablation, 명시적 적합도 레이블, 신뢰구간·효과크기, 파이프라인 로그, 데모. |
| 디자인 | 랭킹 없이도 판단 가능한 테마 색·근거 카드·발견 묶음, 모바일 바텀시트. | 프로토타입과 접근성 점검. |

> **발표 핵심 문장**
> 숨은여행은 공공데이터를 예쁘게 보여주는 지도가 아닙니다.
> 사용자가 좋았던 여행에서 취향을 역산하고, 범용 모델이 구분하지 못하는 관광 경험의 차이를 학습해,
> 아직 가보지 않은 지역을 발견시키고 실제 코스로 연결합니다.

### 15.1 권장 데모 시나리오

1. 사용자가 출발지·기간·분위기·이동수단을 3~5개 칩으로 선택하거나 바로 지도로 진입한다.
2. 숨은여행이 순위를 매기지 않은 지역 후보 3개를 추천 이유와 함께 보여준다.
3. 사용자는 후보를 비교하며 거리·예상 시간·데이터 상태·코스 준비도를 확인한다.
4. 지역을 선택하면 과거 여행과 닮은 점, 새로운 점, 실제 코스 근거를 보여준다.
5. 공식 코스가 있으면 공식 코스를, 없으면 자동 조립 코스를 제시하고 출처를 표시한다.
6. 사용자가 반일·당일·1박 2일 중 하나를 선택해 코스를 저장하고 길찾기·공유로 이어간다.
7. 반복 사용을 원하면 방문 이력을 입력하고 Traveler DNA 기반 발견 묶음을 확인한다.

---

## 부록 A. 용어 정의

| 용어 | 정의 |
|---|---|
| 미방문 지역 | 온보딩 및 이후 기록에서 사용자가 방문했다고 표시하지 않은 시군구. **"비유명"과 동일하지 않다.** |
| 발견 묶음 | 내부 점수를 순위로 노출하지 않고 서로 다른 추천 이유로 구성한 지역 후보 그룹. |
| Region DNA | 지역의 구조화 관광 특징과 의미 임베딩을 결합한 표현. |
| Traveler DNA | 방문 지역·만족도·태그·장소와 이후 행동으로 추론한 연속형 취향 표현. |
| Course DNA | 공식 또는 자동 조립 코스의 장소 구성·의미·공간·신뢰도 표현. |
| Exposure Balance | 서비스 내부 노출 횟수의 편중을 완화하는 요소. **외부 검색량 기반 인기도가 아니다.** |
| Course Readiness | 해당 지역에서 공식 또는 자동 조립 코스를 제공할 수 있는 정도. |
| Human Test | 학습에 사용하지 않고 실제 사람이 작성·평가한 최종 모델 평가셋. |

---

## 부록 B. 현재 결정과 폐기된 결정

| 주제 | 현재 결정 | 폐기·보류 |
|---|---|---|
| 온보딩 | 빠른 조건 칩은 기본, 방문 이력·만족도·태그는 선택형 개인화 | MBTI 12문항 필수 |
| 추천 입력 | 빠른 조건 + 미방문 지역, 반복 사용 시 Traveler DNA | MBTI 유형만 프롬프트에 반영 |
| 코스 | 공식 우선 + 자동 조립 전국 커버 | 공식 코스 단독 |
| AI 학습 | 관광 경험 임베딩 대조학습 | 지역 통계값 전체를 모델로 예측 |
| 노출 개념 | 서비스 내부 Exposure Balance | 미보유 검색량으로 저평가 지수 주장 |
| 후기 | 사진·짧은 글 P1, 핵심 루프 보조 | 숏폼 영상 MVP |
| LLM | 후보 설명·번역, ID 재검증 | 전체 카탈로그 주입 또는 사실 생성 |
| 벡터 검색 | 현재 규모에서 전수 비교 | 초기부터 벡터 DB 도입 |
| 실용성 | 후보 비교·시간/거리·코스 저장·공유를 핵심 경로로 둔다 | 데이터 개수와 지도 탐색만으로 가치를 전달하려는 접근 |
